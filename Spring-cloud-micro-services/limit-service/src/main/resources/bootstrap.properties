spring.cloud.config.uri=http://localhost:8888
spring.profiles.active=qa
management.endpoints.web.exposure.include =*

#-Dspring.profiles.active=dev (load dev properties in another limit-service instance)

#Two ways for reading properties defined in CmsSiteServiceSpringCloudConfiguration and LimitServiceSpringCloudConfiguration
#Both have different properties stored format.